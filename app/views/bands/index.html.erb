<h1>All Bands</h1>

<% @bands.each do |band| %>
<ul>
  <li>
    <%= link_to "#{band.name}", band_url(band), method: :get %>
  </li>
</ul>
<% end %>

<% if signed_in? %>
  <%= link_to "Add a Band", new_band_url %>
<% end %>
